<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.saylik.iteration.ShowIterations">

    <LinearLayout android:id="@+id/iterations_block"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:gravity="center"
        android:orientation="horizontal"
        android:textAlignment="center"
        android:layout_below="@+id/iterations_block"
        android:layout_centerHorizontal="true">

        <TextView android:id="@+id/iterations"
            android:layout_width="126dp"
            android:layout_height="wrap_content"
            android:text="@string/iterations"
            android:textSize="28dp"
            android:layout_gravity="center_vertical" />

        <TextView android:id="@+id/show_iterations"
            android:layout_width="207dp"
            android:layout_height="wrap_content"
            android:textSize="28dp"
            android:paddingLeft="25dp"
            android:text="@string/show_iterations"
            />
    </LinearLayout>

</RelativeLayout>
